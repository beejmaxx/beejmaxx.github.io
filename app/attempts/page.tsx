import type { Metadata } from "next";
import { SiteShell } from "@/components/SiteShell";
import { attempts } from "@/lib/work";
import styles from "./attempts.module.css";
export const metadata:Metadata={title:"attempts",description:"Attempts, rejected paths, unfinished work, and surviving artifacts by bijan.",alternates:{canonical:"/attempts"},openGraph:{title:"attempts — bijan",description:"What I thought, what happened, and what survived.",url:"/attempts"}};
export default function AttemptsPage(){return <SiteShell current="attempts"><main className="page" id="main"><section className="pageHero"><p className="eyebrow">not every unfinished thing failed</p><h1>attempts,<br/><em>with evidence.</em></h1><p className="deck">Working, paused, abandoned, rejected, and superseded are different states. These are annotated by hand.</p></section><section className={styles.list}>{attempts.map((item,index)=><article className="panel" key={item.id}><header><span>{String(index+1).padStart(3,"0")}</span><span>{item.status}</span><h2>{item.title}</h2></header><dl><div><dt>I thought</dt><dd>{item.thought}</dd></div><div><dt>I built</dt><dd>{item.built}</dd></div><div><dt>What happened</dt><dd>{item.happened}</dd></div><div><dt>Decision</dt><dd>{item.decision}</dd></div><div className={styles.survived}><dt>What survived</dt><dd>{item.survived}</dd></div></dl><a href={item.href}>inspect the artifact ↗</a></article>)}</section></main></SiteShell>}
