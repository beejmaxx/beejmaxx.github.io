import type{Metadata}from"next";import Image from"next/image";import{SiteShell}from"@/components/SiteShell";import{getAllCaseStudies}from"@/lib/case-studies";import styles from"./cases.module.css";
export const metadata:Metadata={title:"dossiers",description:"Engineering records with constraints, evidence, limits, and decisions.",alternates:{canonical:"/case-studies"},openGraph:{title:"dossiers — bijan",description:"Systems, with context and evidence.",url:"/case-studies"}};
export default function CasesPage(){const studies=getAllCaseStudies();const lead=studies.find(s=>s.slug==="operations-workstation")??studies[0];return <SiteShell current="work"><main className="page" id="main"><section className="pageHero"><p className="eyebrow">engineering records / not capability claims</p><h1>systems,<br/><em>with context.</em></h1><p className="deck">What exists, what made it difficult, which decisions mattered, and what the available evidence can actually support.</p></section><a className={`${styles.lead} panel`} href={`/case-studies/${lead.slug}`}>{lead.image&&<Image src={lead.image} alt={`${lead.title} interface`} width={1600} height={900}/>}<div><p className="eyebrow">featured dossier / {lead.tags.join(" · ")}</p><h2>{lead.title}</h2><p>{lead.subtitle}</p><span>open dossier →</span></div></a><section className={styles.list}>{studies.filter(s=>s.slug!==lead.slug).map(study=><a href={`/case-studies/${study.slug}`} key={study.slug}><span>{study.index}</span><div><p>{study.tags.join(" · ")}</p><h2>{study.title}</h2></div><p>{study.subtitle}</p><i>↗</i></a>)}</section></main></SiteShell>}
