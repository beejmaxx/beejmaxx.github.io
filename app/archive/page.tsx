import type{Metadata}from"next";import{SiteShell}from"@/components/SiteShell";import{ProjectArchive}from"@/components/ProjectArchive";import{projects}from"@/lib/projects";import styles from"./archive.module.css";
export const metadata:Metadata={title:"archive",description:"The raw public GitHub record: original work, forks, experiments, and old ideas.",alternates:{canonical:"/archive"},openGraph:{title:"archive — bijan",description:"The raw public record, 2010–now.",url:"/archive"}};
export default function ArchivePage(){const originals=projects.filter(p=>!p.fork).length;return <SiteShell current="archive"><main className="page" id="main"><section className="pageHero"><p className="eyebrow">raw public record / 2010—now</p><h1>the unfiltered<br/><em>pile.</em></h1><p className="deck">{projects.length} public repositories: {originals} mine and {projects.length-originals} forks. The useful map is elsewhere; this page keeps the record.</p><p className={styles.mapLink}><a href="/work">want the organized version? →</a></p></section><section className={styles.archive}><ProjectArchive projects={projects}/></section></main></SiteShell>}
